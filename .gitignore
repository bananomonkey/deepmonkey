# Секреты — никогда не коммитим
.env

# База данных SQLite (не коммитим — это рантайм-данные)
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Runtime-файлы, которые бот создаёт сам во время работы (легаси, до перехода на SQLite)
system_prompt.txt
current_model.txt
users.json
chats.json
reply_context.json
user_settings.json

# Telethon-сессия юзербота — содержит доступ к аккаунту, НЕ коммитим
sessions/
*.session
*.session-journal

# Python
__pycache__/
*.pyc
venv/
.venv/

# IDE
.vscode/
.idea/
